# 好友系统本地集成验收记录

## 验收范围

本记录覆盖好友系统与当前 `main` v3.20 的本地集成，不代表真实 CloudBase 已部署，也不代表开发版已经上传。

## 已验证结果

- 合并结果无文本冲突；v3.20 的 AI 提醒订阅鉴权与模板字段、好友系统的账户代次保护和社交入口同时保留。
- 好友系统及相关个人页聚焦回归：`645/645`。
- 整项目串行回归：`977/977`，排除项仅为隔离工作树缺少 Git 忽略的 `project.private.config.json`。
- `git diff --check`：通过。
- 微信开发者工具真实 `auto-preview`：通过；未执行开发版上传。
- 社交云函数使用独立 `cloudbaserc.social.json`，部署配置不保存 `SOCIAL_INVITE_TOKEN_SECRET` 或 `SOCIAL_ADMIN_OPENIDS`。

## 发布候选前仍需执行

1. 获得真实 CloudBase 变更授权。
2. 创建全部社交集合并立即设置客户端不可读写。
3. 创建全部复合索引并等待可用。
4. 安全生成并仅在云函数环境配置 `SOCIAL_INVITE_TOKEN_SECRET`。
5. 安全确认并配置 `SOCIAL_ADMIN_OPENIDS`。
6. 部署 `poker_social` 后回查函数配置、权限和索引。
7. 用管理员、普通用户及第三账号完成邀请、范围、评论、名片、解除好友、断网和账号清除矩阵。
8. 单独取得授权后才可上传开发版。

上述步骤涉及真实资源、账号和密钥；在未取得授权时必须保持未执行状态。
