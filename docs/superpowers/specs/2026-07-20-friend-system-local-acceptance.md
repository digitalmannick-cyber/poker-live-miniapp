# 好友系统本地集成验收记录

## 验收范围

本记录覆盖好友系统与当前 `main` v3.20 的本地集成，以及 2026-07-20 完成的真实 CloudBase 部署回读；不代表开发版已经上传。

## 已验证结果

- 合并结果无文本冲突；v3.20 的 AI 提醒订阅鉴权与模板字段、好友系统的账户代次保护和社交入口同时保留。
- 当前整项目串行回归：`995/995`，`0` 失败。
- 微信开发者工具真实 `auto-preview`：通过，预览包约 `1.80 MB`；未执行开发版上传。
- 社交云函数使用独立 `cloudbaserc.social.json`，部署配置不保存 `SOCIAL_INVITE_TOKEN_SECRET` 或 `SOCIAL_ADMIN_OPENIDS`。
- 真实环境 `cloud1-d3ggy9aq3be912e34` 的 20 个受管集合 ACL 已稳定回读为 `ADMINONLY`，31 个计划索引无缺失；既有外部集合 `hand_actions` 的 ACL 未被修改。
- `poker_social` 已部署并处于 `Active`：`Nodejs20.19`、`index.main`、60 秒、256 MB；邀请密钥、部署提交和代码摘要键均存在，远端提交及代码摘要与当前提交一致。
- 管理员白名单已启用且仅包含已确认账号；幂等部署复核明确返回未修改任何云资源。

## 发布候选前仍需执行

1. 用管理员、普通用户及第三账号完成邀请、范围、评论、名片、解除好友、断网和账号清除矩阵。
2. 对管理员处置至少验证一次固定原因、软删除占位、计数一致、相同 mutation 重试幂等以及普通账号伪造管理员 action 返回 `FORBIDDEN`。
3. 单独取得授权后才可上传开发版。

多账号矩阵涉及真实微信身份，不能用模拟器、无身份 `tcb fn invoke` 或本地 fixture 代替。开发版上传仍须单独取得授权。
