# 2026-07-12 全面代码检测基线

## 环境与版本

- 工作分支：`codex/full-audit-20260712`
- 备份标签：`backup/pre-full-audit-2026-07-12`
- 业务代码基线：`c5babed37a99f4ad283216b9b6f32df21d8ea2ad`
- 检测设计提交：`6914e8e`
- 实施计划提交：`b96c054`
- Node.js：当前工作机 `node`（本轮测试命令实际调用的运行时）
- 检测开始时工作区：无未提交业务改动

## 自动化测试

执行方式：按文件名排序，逐个运行 `node tests/<name>.test.js`，单个失败不终止后续测试。

- 测试文件：65
- 通过：65
- 失败：0
- 超时：0

当前结果证明现有测试在此工作机全部通过，但不证明未覆盖的微信运行时交互、真实云端行为或产品语义正确。

## JavaScript 语法

执行方式：使用 `rg --files -g '*.js' -g '!node_modules/**' -g '!.git/**'` 枚举文件，并逐个运行 `node --check`。

- JavaScript 文件：137
- 语法失败：0

## JSON 与注册路径

### Node UTF-8 JSON 检查

使用 Node.js 以 UTF-8 读取并 `JSON.parse`：`app.json`、`project.config.json`、`cloudbaserc.json`、`sitemap.json`、页面 JSON、组件 JSON 和自定义 tab bar JSON。

- JSON 文件：13
- 解析失败：0

### PowerShell 编码误报说明

Windows PowerShell 的 `Get-Content -Raw | ConvertFrom-Json` 曾把 UTF-8 无 BOM 的中文错误解码为乱码，并报告 `app.json`、`project.config.json` 缺少引号。随后使用以下独立证据排除源文件损坏：

1. Node.js `fs.readFileSync(file, 'utf8')` 与 `JSON.parse` 对两个文件均成功。
2. `Format-Hex` 显示文件字节结构正常。
3. `git diff c5babed^ c5babed -- app.json project.config.json` 中中文及引号正常。

结论：这是 Windows PowerShell 5.1 默认编码造成的检测器误报，不是小程序配置缺陷。后续 JSON 检查统一使用显式 UTF-8 的 Node.js。

### 页面与组件注册

- `app.json` 注册页面：11
- 全局组件：3
- 所有注册页面均存在 `.js`、`.wxml`、`.wxss`。
- 所有全局组件均存在 `.js`、`.json`、`.wxml`、`.wxss`。
- 7 个页面没有同名 `.json`；微信小程序页面 JSON 是可选文件，因此不列为缺陷。

## 基线结论

自动化基线暂未发现失败：65/65 测试通过、137/137 JavaScript 语法通过、13/13 JSON 可解析，已注册页面和组件的必需实现文件存在。下一阶段仍需审计测试未覆盖的数据流、数值边界、云同步失败路径和前端/Agent 责任边界。

## 确认缺陷

当前无。后续发现必须附稳定复现、根因和验证命令。

## 待确认观察项

当前无。
